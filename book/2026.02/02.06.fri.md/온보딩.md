#### Yaml에서의 서비스    
<img width="609" height="374" alt="image" src="https://github.com/user-attachments/assets/dad20d2a-f3c4-4459-9fc8-77af04a1d7ce" />

+ 접속 서비스:클러스터 아이피, 노드포트, 로드발란스, 익스터널라인    
  + service IP=cluster IP , 클러스터 아이피를 지정 안해도 없는 것 아님. 클러스터 아이피와 Node Port,Load Balance는 포함관계 이므로 하나를 지정 해도 다른 두개도 있는 것
  + 단, External line은 외부, DNS 역할이기 때문에 별개이다.
+ Selector Name으로 모두 찾음 : IP는 계속 바뀌기 때문에 이름으로

#### Kind 객체
+ 아이피는 계속 바뀌니까 Match Label로 찾음
+ template을 가지고 파드를 만듦
+ 템플릿으로 안하고 따로 파드를 만들면 관리를 안 함 라벨을 달아서 app:web 같아야지만 관리가 됨 (이름도 겹치면 안되기때문에 해시값을 따로 단다)
+ replicas 수 만큼 파드를 유지 함
+ Service를 안하면 접속 자체가 안 될 것?
+ 스토리지는 엄청 큼 : 스토리지 중에서 덩어리 나눈 게 크게 볼륨을 나눈다고 표현 함
+ 인그레스는 L7 데이터 : 사람이 보고 읽을 수 있는 데이터 URL,URI로 로드 발란싱 함 (파드로 직접 보냄)

+ k8s에는 도커이미지가 아닌 엔진들 들어있음
+ 대부분의 이미지는 도커 이미지에서 가져옴

#### 실습 준비 (4명)
+ 통일하기
  + master
  + 12345678

+ ssh키 만으로 프롬프트 떨어지게 하기: 패스워드 체크박스 표시 지워야 함
  + sudo 패스워드 안 넣게 설정하기
  + 키 만들어서 넣어주고 서로 join하기
# sudo visudomaster 로 sudoers 열어서 마지막 줄에 명령어 추가하기
