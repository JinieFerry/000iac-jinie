아래 항목 중 하나라도 정상 상태가 아니면 실습이 꼬입니다.

1. 노드 상태 확인 : `kubectl get nodes`
   + 모든 노드 Ready : j:vmmaster1,vm1
  
2. API 서버 확인 : kubectl cluster-info
   + 정상 응답 : https://<MASTER-IP>:6443 <- 열어둔 포트가 떠야 함
  
3. swap off : `swapof --show` <- sawp on을 출력하는 명령으로 출력이 없어야 정상 상태
4. 방화벽/포트:
   + NodePort 30080 열려있어야 함
   + 클라우드 / VM 환경 보안 정책 확인
5. 이미지 풀 테스트
```
kubectl run test --image=nginx --restart=Never
kubectl delete pod test
```
